# ROADMAP · 功能差距清单（带解释版）

> **这份文档是什么**：对比三个参考项目，列出我们的博客还没有的功能和动效，每一项都用大白话解释清楚"这是什么东西、长什么样、在哪个网站能看到"。以后想加哪个，翻这份文档挑就行。
>
> **对比对象**：
> - **WitchCat** — [witchcat.cn](https://www.witchcat.cn/zh)，闭源网站（就是 `C:\blog\OTHER_FEATURES.md` 那份调研笔记的蓝本）
> - **Kirameku** — 源码在 `C:\blog\Kirameku-main`，实机演示 [boke.hiromu.top](https://boke.hiromu.top)
> - **XinghuisamaBlogs**（下称 XHBlogs）— 源码在 `C:\blog\XinghuisamaBlogs-main`，实机演示 [xinghuisama.top](https://www.xinghuisama.top)
> - **anze.love** — 额外参考，主要看它的左下角小人
>
> 调研日期：2026-08-28（四个站都实地打开核对过）
> 工作量含义：**S** = 半天内 / **M** = 一到两天 / **L** = 三天以上

## 先列一下我们已经有的（不用重复做）

首页轮播 Banner + 打字机副标题、资料卡、节气公告栏（自动显示"处暑 · End of Heat"这种）、统计数字滚动；文章列表双视图 + 分类标签筛选（带数量角标）+ 分页；文章详情（代码高亮、数学公式、提示框、右侧目录、评论、阅读量、上一篇下一篇）；归档时间线、说说、相册（点相册 → 照片堆扇形展开 → 拍立得照片墙 → 点开大图灯箱）、友链、关于页、RSS 订阅、站内搜索；粒子主题系统（白天樱花/晚上萤火虫自动切换，也可手动选樱花/萤火虫/落叶/下雪/关闭）、点击页面迸发彩色粒子、第一次进站的开场动画、页面切换淡入、滚动时内容逐个浮现、文章卡片跟随鼠标 3D 倾斜、导航栏当前页指示小滑块；完整写作后台。

---

## A. 内容模块（整块的新页面/新板块）

### A1. 项目展示页 `/projects` —— M
**是什么**：一个专门展示"我做过的项目"的页面，每张卡片像一个迷你名片：项目名、一句话介绍、进行中还是已完成、用的编程语言（能显示"C# 74.2%"这种占比条）、GitHub 星标数，点击跳转到仓库。
**在哪能看到**：WitchCat 首页右下"最新动态"里的项目卡就来自这个页面；Kirameku 和 XHBlogs 都有独立 `/projects` 页。

### A2. 音乐馆 + 底部全局播放器 —— L（三个站都有，属于"镇站之宝"级）
**是什么**：两件事。①**音乐馆页面**：管理多个歌单、按流派分类、显示最近播放过的歌（记在你自己浏览器里，不用账号）、歌词滚动显示。②**底部全局播放器**：屏幕底部一条常驻迷你控制条，有播放/暂停、上一首下一首、进度条。**关键体验是：你在任何页面之间跳转，音乐都不会断**——现在大多数网站一换页背景音乐就停了，这个不会。
**黑胶唱片机动画**：播放器待机时显示"等待唱片入场"，选歌后一张黑胶唱片旋转起来，是拟物的小动画。
**在哪能看到**：WitchCat 右下角有整条播放器（可以最小化）；xinghuisama.top 首页有网易云卡片和歌词；Kirameku 源码用的是 @meting（一个读取网易云歌单的接口）。

### A3. 留言板 `/messages` —— S~M
**是什么**：一个独立页面，游客可以留下昵称 + 一句话/头像，像网站的"涂鸦墙"，大家留言互相可见。和文章底部的评论区不同——它不属于任何文章，就是给整个站留言打招呼的地方。
**在哪能看到**：anze.love 的"留言板"菜单；Kirameku 的 `/messages`。

### A4. 追番清单 —— M
**是什么**："追番"= 追动画。一个表格/卡片页列出你在看的番剧，每部有封面、简介、你的评分、状态（追番中/已看完/想看/已弃）、观看进度（比如"25/28 集，85%"的进度条），点击封面直达 B 站播放页。侧边还有统计：几种状态各多少部、最常出现的类型标签。
**在哪能看到**：WitchCat（菜单"我的"分组里）。数据就是我们后台维护一个 JSON，前台渲染。

### A5. 游戏库 —— M
**是什么**：同 A4，对象换成游戏：想玩/在玩/已通关/搁置/弃坑几种状态、累计游玩时长统计、点击跳 Steam 商店页。WitchCat 是直接对接你的 Steam 账号自动同步，我们可以先手动维护，以后写脚本拉 Steam 数据。

### A6. 日记 —— S~M
**是什么**：比"说说"正式、比"文章"随意的日常记录，每篇可以标注**当天天气和心情**（存在文章开头的数据里），显示"258 天前"这种相对时间，按年份归档。
**在哪能看到**：WitchCat 的日记板块。

### A7. 杂谈/短文板块 —— S
**是什么**：介于说说和文章之间的"短文"：有封面图、能写千八百字，但不进主博客列表。适合写"今天把音乐模块做完了"这种记录。
**在哪能看到**：xinghuisama.top 首页的"Records"卡（"音乐板块完成"那条就是杂谈），独立菜单"杂谈"。

### A8. 技能树（"知识大脑"） —— M
**是什么**：像游戏里的天赋树：一个中心节点（比如"前端"）点开分出子节点（React / CSS / ...），再点再分（React 下有 Next.js、状态管理...），每个节点标注你的熟练度（专家/高级/中级/初级）和使用年限。整体可以"全部展开"，还有统计面板（各级别分布、平均经验）。
**在哪能看到**：WitchCat 的"我的"分组。

### A9. 成长时间线 —— S
**是什么**：和我们已有的"归档"（按时间列文章）不同，这个是**个人履历视角**：2023 年上大学、2024 年开始实习、2025 年做了某项目……每段有类型标记（教育/工作/项目/成就）、持续了多久、在哪个城市、用了什么技术，支持按类型筛选。
**在哪能看到**：WitchCat 首页"最新动态"里那条"2025年11月 个人博客项目"就是时间线里的一条。

### A10. 赞赏/赞助页 —— S
**是什么**：放支付宝/微信收款二维码的页面，配"三步打赏指引"（1 打开支付宝 2 扫码 3 输入金额）、历史赞助者鸣谢名单、免责声明（"自愿赞助，服务不因赞助变化"）。
**在哪能看到**：WitchCat 的"其他"分组。

### A11. 收藏夹/书签页 —— S~M
**是什么**：自己常用网站的个人导航页：按分类（工具/学习/娱乐）整理一堆网站卡片，点击直达。因为是自己后台维护的，可以当成"我的浏览器书签同步页"。
**在哪能看到**：Kirameku 的 `/bookmark` 菜单"收藏夹"。

### A12. 小说阅读器 —— M
**是什么**：接入第三方小说源的在线阅读器：搜索书名 → 打开目录 → 逐章阅读，带阅读进度记忆。⚠️ 注意第三方源的版权风险，做之前想清楚。
**在哪能看到**：Kirameku 菜单"小说"。

### A13. 实验室/花园页 —— L
**是什么**：Kirameku 的"整活合集"，一个 `/garden` 分区塞了 19 个纯玩页面：烟花（点击放烟花）、流体模拟（手指划过像搅动水面的彩色烟雾）、太阳系 3D（可拖拽旋转的星空）、代码雨（黑客帝国那种绿色字符雨）、万花筒、重力沙（像"沙子游戏"App）、生命游戏（细胞自动动死繁衍）、排序算法可视化（看着柱子被排好）、数学函数绘图、地图、二维码生成器、JSON 格式化工具……
**建议**：不用全做，挑 3-5 个视觉最炸的（烟花/流体/星空/代码雨）做一个"实验室"菜单。
**在哪能看到**：Kirameku（hiromu 站）首页往下翻，"garden"入口。

---

## B. 布局与导航

### B1. 常驻侧边栏布局 —— M
**是什么**：现在我们的首页是"一列卡片从上到下"。WitchCat 是**三栏**：中间是文章列表，**右边一列从上到下固定放着**：站长卡（头像+社交图标）、公告（节气）、分类列表（带数量）、标签云、最新动态、RSS 订阅框（一键复制链接）、音乐迷你卡。屏幕大的读者不用滚动就能看到所有快捷入口。
**在哪能看到**：witchcat.cn 首页右列；Kirameku/XHBlogs 首页是简化版（顶部导航 + 首页聚合卡片）。

### B2. 多级下拉菜单 —— S
**是什么**：顶部导航的分组折叠：像 WitchCat 那样"链接 ⌄""我的 ⌄""关于 ⌄"——点开弹下拉子菜单。功能页面多了以后（现在我们 7 个菜单项还行，加上追番/游戏/赞助就 10 个了），分组比平铺清爽。

### B3. 首页"最新文章"大图轮播卡 —— S
**是什么**：现在我们首页最新文章是 3 列小卡片；这个是**一张横跨全宽的大卡片**：左边大图右边标题摘要，自动轮播 + 左右箭头/圆点手动切换。
**在哪能看到**：xinghuisama.top 首页"Latest Insight"卡；Kirameku 首页同款。

### B4. 首页照片墙海报卡 —— S
**是什么**：首页放一张"最新相册"的海报卡（相册封面 + 标题 + 一句话简介），点击直达相册页。让相册入口从菜单里的一行字变成有图片的诱人卡片。
**在哪能看到**：xinghuisama.top 首页"二六年南昌五一摄影"卡。

### B5. 首页说说轮播卡 —— S
**是什么**：首页一张小卡轮播展示最新几条说说（"一个月左右，终于到 100 star 啦~"这种），让说说板块在首页有露出。
**在哪能看到**：Kirameku/hiromu 首页的 chatter 轮播。

### B6. 系统运行时长 + 技术栈徽章 —— S
**是什么**：页脚或首页一行小字："系统已稳定运行：158 天 15 小时"（从部署那天开始计时，每秒跳动），旁边几个小徽章 Next.js / React / Tailwind。极客味很足的小细节。
**在哪能看到**：xinghuisama.top 页脚（跳动的时钟）；WitchCat 仪表盘。

### B7. 日历组件 —— S~M
**是什么**：右上角一个日历图标，点开弹出月历（可翻月），可以标记"这天发了文章"。WitchCat 首页右上角有个 Calendar 按钮。

### B8. 主题色选择器 —— M
**是什么**：我们已经有"明/暗"切换；这个是**换颜色**：一键把全站的主色从蓝紫色换成粉色/绿色/橙色（按钮、高亮、指示条全部跟着变）。WitchCat 右上角"主题色"按钮，点开一排色块。
**实现要点**：把主色做成 CSS 变量就能全站生效。

---

## C. 动效与特效（我们的强项区）

### C1. 看板娘（Live2D 小人） —— M~L（anze.love 左下角那个就是它）
**是什么**：屏幕角落站一个**动漫小人**，用 Live2D 技术（让 2D 立绘像纸片人一样自然摆动、眨眼、说话时嘴动）。她会待机摇摆、用鼠标戳她会有不同反应和台词气泡、可以点击换人/换装，不想要了一键隐藏。
**技术**：anze.love 用的是"pio 方案"（pixi.js 渲染 + Cubism 4 内核），Kirameku 直接放了一套现成脚本在 `public/live2d/`；网上有免费模型和开源轮子（如 pixi-live2d-display）。
**在哪能看到**：anze.love 左下角（你说的那个小人）；witchcat.cn 左下角（有"隐藏看板娘"按钮）；hiromu 站左下角。

### C2. AI 聊天助手 —— M
**是什么**：屏幕角落一只可以聊天的猫（或任意形象），点开气泡输入框，接入 AI 接口（XHBlogs 用的是 Gemini）就能对话，还能设定人设（"你是本站的看门猫"）。摸它会出表情。
**在哪能看到**：xinghuisama.top 右下角"聊天"按钮。

### C3. 弹幕背景 —— S（就是从屏幕飘过的那行字）
**是什么**："弹幕"= B 站视频里从右往左飘过的评论文字。**弹幕背景**就是把这种飘字当成**背景装饰**：站主在后台配置一串短语（比如"今天背单词了吗？""BUG 修复进度 99%""睡大觉中"），它们就以不同速度、不同高度、半透明地从屏幕右边慢慢飘到左边，**不挡任何点击**，纯氛围。
**在哪能看到**：xinghuisama.top 首页（仔细看背景里有字在飘）；B 站视频的评论区画面就是同款概念。

### C4. 鼠标拖尾 —— S
**是什么**：鼠标在屏幕上移动时，身后拖出一串**发光的蓝紫色小光点**，跟着下坠、渐渐消散，像萤火虫尾迹。只对鼠标生效，不影响任何内容。
**在哪能看到**：Kirameku 源码 `components/ui/MouseTrail.tsx`（实机 hiromu 站默认好像没开全，源码里有完整实现）。

### C5. 文字选中星光 —— S
**是什么**：你用鼠标**选中一段文字**（拖蓝）松开的那一刻，选区上方"啵"地绽开几颗**四角小星星**，一闪即逝。纯粹的可爱细节。
**在哪能看到**：Kirameku 源码 `components/ui/KiraSparkle.tsx`（站名"きらめく"=闪烁，就是从这来的）。

### C6. 季节自动粒子 —— S
**是什么**：我们现在的粒子主题是"手动选"或"跟日夜走"；这个再加一档"**跟季节走**"：1-3 月自动樱花、4-6 月萤火虫……Kirameku 是按月份自动切换春樱/夏萤/秋叶/冬雪。
**实现**：我们已有四种粒子组件，只差"按月份自动选"的逻辑，很便宜。

### C7. Logo 连击彩蛋 —— S
**是什么**：连续点击左上角站名 Logo 7 次，全屏爆**彩纸雨**（600 个彩色小纸片分三波落下），配一个玻璃质感的提示气泡。彩蛋，懂的人会心一笑。
**在哪能看到**：Kirameku/hiromu 站，连点左上角 Logo 试试。

### C8. 页面切换"传送门"遮罩 —— S
**是什么**：点一个菜单跳页时，不是瞬间换内容，而是先盖上一层**毛玻璃遮罩**，中间三个圆环反向旋转（像传送门/加载圈），配一句随机小贴士（"小贴士：右下角可以关特效哦"），400ms 后揭开新页面。
**说明**：Kirameku 源码里写好了这个组件但实际没启用；我们已有一层页面淡入，这个是更"重"的版本。

### C9. 友链"漂流瓶" —— M
**是什么**：友链页不再是整齐网格，而是所有友链卡片**随机散落在页面上、各带随机角度**，像一堆漂流瓶/贴纸；你可以**用鼠标拖着它们挪位置**玩，点击卡片弹出详情。视觉记忆点极强。
**在哪能看到**：Kirameku/hiromu 站的"友链"页（试试拖动卡片）。

### C10. 归档"时间河" —— M
**是什么**：归档页的时间轴不是一根直线，而是**一条用 SVG 画出来的弯曲河流**（几层正弦波叠加，像水流），文章节点像石头一样分布在河的两岸，整个区域可以**横向拖拽**顺流而下，越久的文章越往"下游"。
**在哪能看到**：Kirameku/hiromu 站的"归档"页。

### C11. 风中草粒子层 —— S
**是什么**：屏幕**最底部**长出一排（150 根）渐变色**小草**，随风左右摇摆，白天是绿色、夜里变白色剪影。和樱花/萤火虫叠加很有层次。
**在哪能看到**：xinghuisama.top 页面底部边缘；XHBlogs 源码 `WindyGrass.tsx`。

### C12. WebGL / Three.js 创意页 —— L
**是什么**：用浏览器的 3D 技术（Three.js）做一个沉浸式页面。XHBlogs 的"灵境"页：一片自定义光点星空里悬浮着 3D 模型，配上**游戏化系统**——你有等级、看文章得经验、解锁徽章（六边形徽章带流光）。属于"炫技天花板"，工期也最长。
**在哪能看到**：xinghuisama.top 菜单"灵境"。

### C13. 图表统计仪表盘 —— M
**是什么**：用图表库（recharts）把站点数据画成**饼图/折线图/柱状图**：分类占比饼图、每月发文量折线、阅读量趋势，数字还会滚动跳到目标值（AnimatedNumber）。放在首页或后台都行。
**在哪能看到**：Kirameku 首页的"统计"卡（recharts 饼图）。

---

## D. 交互小玩具

### D1. 径向工具菜单 —— M
**是什么**："径向菜单"= 环形菜单：长按键盘 Tab（或长按屏幕），在光标周围**呈扇形弹出一圈图标按钮**（计算器/二维码/小游戏…），往哪个方向划就选中哪个。游戏里常见的快捷轮盘 UI。
**在哪能看到**：Kirameku 源码 `components/ui/RadialMenu.tsx`（长按 Tab 呼出，含内置小游戏）。

### D2. 快捷小工具箱 —— S~M
**是什么**：右下角工具按钮点开一排实用小工具：**计算器**、**二维码生成器**（输入网址出二维码）、**JSON 格式化**（把挤成一团的接口数据排版成缩进好看的样子）、Markdown 转换等。自己网站自带瑞士军刀。
**在哪能看到**：Kirameku 的 garden 里（独立页版）；XHBlogs 右下角工具箱。

### D3. 天气小组件 —— S
**是什么**：首页/侧栏一张小卡显示你所在城市的实时天气（图标 + 温度），调用免费天气接口。
**在哪能看到**：XHBlogs 源码 `app/api/weather`（实机好像没开）；WitchCat 仪表盘里有。

### D4. 一言卡 / 舔狗日记 —— S
**是什么**：一张小卡片显示**随机一句话**——接"一言"（hitokoto）API 就是随机名言/台词/歌词；Kirameku 首页那张是"舔狗日记"（每次点"换一条"刷新一条搞笑深情文学）。纯快乐源泉，还给首页加了"每次刷新都不一样"的活气。
**在哪能看到**：hiromu 首页"舔狗日记"卡（有"换一条"按钮）。

### D5. GitHub 访客登录（OAuth） —— M
**是什么**："OAuth"= 授权登录。游客点"用 GitHub 登录"，跳到 GitHub 授权页点一下同意，回来就有身份了（头像+昵称），可以用这个身份留言、点赞，不用注册账号。需要后端配合，适合二期。
**在哪能看到**：Kirameku 的 `/auth/callback` 路由。

---

## E. 基建 / 后台 / SEO

### E1. 多语言（i18n） —— L
**是什么**："i18n"是 internationalization 的缩写（i + 18个字母 + n），指多语言支持：右上角切中文/English/日本語/한국어，全站文案跟着换。WitchCat 有四语。**工作量巨大且个人博客收益存疑，建议最后做或不做**。

### E2. 站长数据仪表盘 —— M
**是什么**：给站主自己看的数据页：服务器**运行时长/内存占用/今日请求数**、**热门文章 Top5**。我们后台已有内容统计（文章数/阅读量），缺"服务器运行状态"这块，可以接 umami（一个开源自托管的访问统计工具，Docker 一条命令就能跑）。
**在哪能看到**：WitchCat 菜单"仪表盘"。

### E3. 说说评论 —— M
**是什么**：让每条说说下面也能像文章一样被评论。Kirameku 的说说甚至有评论互动通知。纯静态方案做不了，我们的全栈架构做这个很顺。
**在哪能看到**：Kirameku 的说说页。

### E4. 相册浏览量 —— S
**是什么**：每张照片记录被点开看过的次数，相册卡片上显示"被看了 N 次"。我们已有文章阅读量统计，照搬到照片上就行。

### E5. 云存储（OSS 图床） —— M
**是什么**："OSS/图床"= 云端图片仓库（阿里云 OSS、腾讯云 COS 等）。我们现在的图片存在服务器自己硬盘上（`public/uploads/`）；换 OSS 后图片走 CDN 加速、换服务器不用搬图片。我们的上传接口已经做了抽象，加一个 OSS 驱动即可。
**在哪能看到**：Kirameku 后端就是阿里云 OSS。

### E6. CC 协议声明 —— S
**是什么**："CC BY-NC-SA 4.0"= 知识共享协议（署名-非商业性-相同方式共享）：告诉别人"我的文章可以转载，但要署名、不能商用、改编后要同样开放"。页脚放协议图标和链接。我们现在只有普通版权行。
**在哪能看到**：witchcat.cn 页脚 "CC BY-NC-SA 4.0"；XHBlogs 页脚同款。

### E7. Sitemap / robots —— S
**是什么**：两个给搜索引擎看的小文件：**sitemap.xml**（站点地图，列出所有页面地址，帮 Google/百度更快收录）、**robots.txt**（告诉爬虫哪些能抓哪些不能）。我们有 RSS 了，补这两个 SEO 就齐了。

### E8. 代码块配色跟随主题色 —— S
**是什么**：现在文章里的代码颜色跟"明/暗"模式走（亮色浅底、暗色深底）。可以再加一档：配色跟着 B8 的主题色走（选了粉色系就换一套粉色系代码皮肤）。锦上添花。

---

## 建议补齐顺序（个人推荐）

1. **快赢组**（每项半天内，观感提升最大）：B3 首页大图轮播卡 → C4 鼠标拖尾 → C5 选中星光 → C3 弹幕背景 → B6 运行时长徽章 → C7 Logo 彩蛋 → D4 一言卡 → E6 CC 协议 + E7 sitemap
2. **模块组**（补内容版图）：A1 项目页 → A7 杂谈 → A3 留言板 → A4 追番 + A5 游戏库 + A6 日记（凑齐 WitchCat"生活四件套"）
3. **镇站之宝组**（大件，一次做一个）：A2 音乐馆+全局播放器 → C1 看板娘 → C2 AI 聊天 → C13 图表仪表盘 → B8 主题色
4. **炫技组**（有余力再玩）：C9 漂流瓶友链 → C10 时间河 → A13 实验室 → C12 Three.js 页 → E1 多语言

## 参考资料索引

- WitchCat 实机：https://www.witchcat.cn/zh （布局/功能蓝本，无源码）
- Kirameku 源码：`C:\blog\Kirameku-main`（前端在 `Kirameku/`，FastAPI 后端在 `Kirameku-backend/`；动效组件集中在 `Kirameku/components/` 的 ui、layout、posts 子目录）
- Kirameku 实机：https://boke.hiromu.top
- XHBlogs 源码：`C:\blog\XinghuisamaBlogs-main`（前台在 `XHBlogs/`，本地写作后台在 `my-blog-manager/`；动效在 `XHBlogs/components/`）
- XHBlogs 实机：https://www.xinghuisama.top
- Live2D 看板娘参考：https://anze.love/留言板（pio 方案：pixi.js + Cubism 4 SDK，页面源码里的脚本路径可直接参考）
